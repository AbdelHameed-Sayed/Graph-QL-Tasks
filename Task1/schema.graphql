type Post {
  id: ID
  title: String
  author: String
  comment: String
}

type Query {
  posts: [Post]
}

type Mutation {
  createPost(title: String, author: String, comment: String): Post
  deletePost(id: Int): [Post]
}
